#ifndef PADDLE_HPP_
#define PADDLE_HPP_

#include "screen.h"
#include "ball.h"

PROGMEM const unsigned char ca1[] = {
  16, 8, //pictureresolution
  //0x3E,0x7E,0x7E,0x7E,0x7E,0x7E,0x7E,0x7E,0x7C,0x3F,0xC0,0x03,0xC0,0x03,0x80,0x01

  0x7E,0x3E,0x7F,0x7E ,0x7F,0x7E  ,0x7E,0x7E ,0x3F,0x7C ,0x03,0xC0,0x03,0xC0, 0x01,0x80

  //0x01,0x80,0x03,0xC0,0x03,0xC0,0x3F,0x7C,0x7E,0x7E,0x7F,0x7E,0x7F,0x7E,0x7E,0x3E

  
};

class Paddle
{
private:
    float pos_x;
    int16_t pos_y=10;
    int16_t max_x, max_y;
    const int16_t paddlewidth=32;
    int16_t paddleheight=4;
    uint16_t color = ILI9486_YELLOW;
    bool justLine=true;

    Screen * tft;


public:
    Paddle(Screen * mastertft);
    void setType(bool gametyp);
    void update(float ballpos);
    void move(int16_t movex);
    void draw(void);
    void undraw(void);
    int16_t getX(void);
    void setX(int16_t x);
};

#endif // PADDLE_HPP_